'use strict';

/**
 * @ngdoc function
 * @name yeomanTestApp.directive:BeaconList
 * @description
 * # beaconList
 * Directive of the yeomanTestApp
 */
 /* CAN DELETE
function beaconList() {
    return {
		restrict: 'E',
		replace: true,
		scope: true,
		template: [
			'<ul class="dynamic">',
				'<li>',
					+	'Major: ' + beacon.major + '<br />'
					+	'Minor: ' + beacon.minor + '<br />'
					+	proximityHTML(beacon)
					+	distanceHTML(beacon)
					+	rssiHTML(beacon)
				'</li>',
			'</ul>'
		].join('')
	};
} 

angular.module('yeomanTestApp')
  .directive('beaconList', beaconList);
*/